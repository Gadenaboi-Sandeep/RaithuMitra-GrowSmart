<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_main_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_main_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.RaithuMitra"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".ui.auth.SignupActivity" />
        <activity android:name=".ui.main.MainActivity" />
        <activity
            android:name=".ui.equipment.AddEquipmentActivity"
            android:parentActivityName=".ui.main.MainActivity" />
        <activity
            android:name=".ui.labor.CreateLaborProfileActivity"
            android:parentActivityName=".ui.main.MainActivity" />
        <activity android:name=".ui.contract.CreateContractActivity" />
    </application>

</manifest>
